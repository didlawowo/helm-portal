server:
  port: 3030

storage:
  path: "data"

backup:
  enabled: true
  provider: "gcp" # "aws" ou "gcp"
  gcp:
    bucket: "helm-portal-backup"
    projectID: "dc-consulting-home"
  # aws:
  #   bucket: "helm-portal-backup"
  #   region: "eu-west-1"

logging:
  level: "info"
  format: "text"
